a
##Probando asociatividad de operadores
program
  ## Los parentesis se preservan
  x := ((y = z) < u) <> ((k>v) <= (z >=q));
  ## Los parentesis se eliminan
  x := (((a + b) - c) - d) - - -e;
  ## Los parentesis se preservan
  x := a + (b - (c + (d - - -e)));
  ## Los parentesis se preservan
  x :=  (a | b) | (c | d);
  ## Los parentesis se eliminan
  x := (((a * b) / c) % d) * **e;
  ## Los parentesis se preservan
  x := a * (b / (c % (d * **e)));
  ## Los parentesis se eliminan
  x := ((a.b)[c])->d;
  ## Los parentesis se eliminan
  x := (*a)[b]->c.d;
  ## Los parentesis se preservan
  x := *(a[b]->c.d)
end_program


